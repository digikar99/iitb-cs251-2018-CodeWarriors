
coord_list = []

with open('students.txt', 'r') as loc_file:
    var_list = loc_file.read().split()
    n = int(var_list[0])
    global coord_list
    for i in range(1,2*n,2):
        coord_list.append((int(var_list[i]), \
                           int(var_list[i+1])))

    #print(coord_list)

## Make a boolean recursive function to determine if
## it there is a "clear" path from s1 to s2
def path_is_clear(x1,y1,x2,y2):
    # code it
    
def find_loc(x1, y1, x2, y2):
    if (x1 <= x2 and y1 <= y2):
        # use path_is_clear function
        
    else:
        return -1


## Test whether s1 and s2, indeed lie in coord_list
